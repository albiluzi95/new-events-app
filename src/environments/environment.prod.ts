export const environment = {
  production: true,
  url:'http://localhost:5730/',
  events:'events/',
  voters:'voters/',
  user: 'user/'
};

// url is temporary redirected at localhost becouse is not deployed in production yet.